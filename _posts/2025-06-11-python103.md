---
layout: post
title: Python for AI 103
date: 2025-06-15
description: list
tags: coding AIO
categories: AI
giscus_comments: true 
pretty_table: true
toc:
    sidebar: right
---

:round_pushpin: **Acknowledgement:** This post is based mainly on Dr. Quang-Vinh Dinh's presentation and slides and partly on peers' responses from the course, AIO2025, offered by AI VIETNAM.

:thought_balloon: **Relevant information and opinions:** The audience of this lecture were those who learned [Python for AI 102](/_posts/2025-06-12-python102.md), have some coding experience, and know a bit of AI knowledge. It is reccommended that complete newbies should read the materials and spend time practicing writing code without the assistance of AI over and over again to improve their coding and code-reading skills. Note that only necessary Python knowledge is introduced here.

> Think about anything you learn as a game. A game always has rules. We just need to follow the rules to solves problems and conquer the game. <br> <br>
> When learning programming, don't trust anyone or any book. Only be convinced that a program is correct once you have already tried it yourself and seen the expected result. <br>
> -Dr. Quang-Vinh Dinh

In today's post, we'll delve into list in Python. We won't approach it like computer scientists where they actually know how to integrate it into embedded systems so that physical devices can perform specific tasks. Instead, we'll approach it from a programming viewpoint.

<hr>

:pushpin: **Pro tip:** When learning a new concept in programming, read documentation and try out different kinds of examples to truly understand it. 

### :bricks: **Data Structure**
Data structures were created in order for computers to handle data faster. Such speed is not so significant in practice these days since computers' hardware is upgraded every so often.

A data struture is a format used to organize and store data for efficient data access and management. It also reflects on real life situations, for instance, making a dictionary, a customer lineup scenario, or constructing a family tree. 

There are two types of data structures and they are arranged according to their usage frequency from top to bottom. 

| Built-in   | User-defined |
|:----------:|:------------:|
| List       | Stack        |
| Dictionary | Queue        |
| Tuple      | Tree         |
| Set        | Graph        |
|            | Linked List  |

<hr>

## :mag_right: **1D List**
```python
list_name = [element_1, ..., element_n]
```
A list is a container of elements. As mentioned before a list can consist of different kinds of data representation, but we use just one type for each list in practice, especially numbers since computers only works with them.
```python
list_0 = [] # an empty list
list_1 = [7, -5, 8]
list_2 = [5, 4.7, -2, "hello", 0, True, -1000.1, 5, 5, 5]
list_3 = ["aivietnam", [2, -3, 0, 5]]
```
The rules for list are:
- All elements are placed inside a pair of brackets.
- Every two consecutive ones are separated by a comma.
- As soon as a list is created, so are its indices.
    - :arrow_right: **Forward index:** starts at $0$ and increases by $1$
    - :arrow_left: **Backward index:** starts at $-1$ and decreased by $1$.
    - `list_name[index]` is used to access an element with `index` is less than the number of elements. 

We mainly use forward index and maybe, when retrieving the last element, $-1$ from backward index.
```python
list_1 = [7, -5, 8] 
list_1[0]
>>> 7
list_1[-1]
>>> 8
list_1[9]
>>> IndexError
```
There are more rules in this list game coming up now.
### :heavy_check_mark: **Operators**
#### :new: **Updating an Element**
Lists are **mutable**, meaning their content can be changed after creation. Mutable and immutable are coming soon in this post. 
```python
list_1 = [7, -5, 8] 
list_1[2] = 10000
list_1
>>> [7, -5, 10000]
```

#### :hocho: **Slicing**
Similar to the **range(start, stop, step)** function, the **slicing** syntax of list is `list[start:end:step]`.
- *start*, *end*, and *step* are indices. Their values are all optional. 
- *start* is $0$ and *step* is $1$ by default.
- That *end* is not specified means until the end on either side depending on *step*.

```python
a_list = [3, 4, 5, 6, 7, 8, 9, 10]
a_list[:3]
>>> [3, 4, 5]
a_list[6:]
>>> [9, 10]
a_list[0:1000:3]
>>> [4, 7, 10]
a_list[::-1]
>>> [10, 9, 8, 7, 6, 5, 4, 3]
a_list[3:1]
>>> [] 
```

<details class = "bordered-block">
    <summary>
        <strong>Practice Exercises</strong>
    </summary>
    <div class = "inside-bordered-block language-python">
        <strong>Given the list <code>animal = ['e', 'l', 'e', 'p', 'h', 'a', 'n', 't']</code>. Use slicing in different ways if possible to answer the following.</strong>
        <ol>
            <li> Get the first four letters. </li>
            <li> Get the last three letters. </li>
            <li> Get the every second letter starting from index 1. </li>
            <li> Get the all letters except the first and last. </li>
            <li> Get the middle three letters. </li>
            <li> Get an empty list. </li>
            <li> Reverse the letters. </li>
            <li> Get the letters h, p, e . </li>
            <li> What does <code>animal[1:-1:2]</code> return? </li> 
        </ol>  
    </div>
</details>

#### :heavy_plus_sign: **$+$ and $*$ Operators**
The $+$ operator is to concatenate lists. The $*$ operator is to make multiple copies of a list and concatenate them one by one. The original list remains unchanges unless it is assigned to the addition or multiplication.
```python
list_1 = [7, -5, 8]
list_2 = [5, 4.7, -2, "hello", 0, True, -1000.1]
addition = list_1 + list_2
addition
>>> [7, -5, 8, 5, 4.7, -2, "hello", 0, True, -1000.1, 5, 5, 5]
multiplication = list_1 * 3
multiplication
>>> [7, -5, 8, 7, -5, 8, 7, -5, 8]
```
The $*$ operator can be used when you want to initialize a list with some default value. For example, a teacher is making a file to input students' quiz grades later. They will do `quizzes = [-1] * 50` because they can avoid typing one number too many times and ensure a student's grade will not have been put in yet as they see the number. 

### :heavy_check_mark: **Mutable and Immutable**
<details class = "bordered-block">
    <summary>
        <strong>Practice Exercises</strong>
    </summary>
    <strong>Point out the differences between the functions below. Is the original list updated in both? Explain why or why not. How is it related to immutable and mutable here? What are pros and cons of each? In which cases should we use them?</strong>
    <table class = "language-python">
        <tr>
            <th>Immutable</th>
            <th>Mutable</th>
        </tr>
        <tr>
        <td>
<code class = "multi-line-code">
    def square(data):
        result = []
        for value in data:
            result.append(value * value)
        return result
    # test
    data = [6, 5, 7, 1, 9, 2]
    print(data)
    data_s = square(data)
    print(data_s)
</code>
        </td>
        <td>
<code class = "multi-line-code">
    def square(data):
        length = len(data)
        for i in length:
            value = data[i] * data[i]
            data[i] = value
    # test
    data = [6, 5, 7, 1, 9, 2]
    print(data)
    data_s = square(data)
    print(data_s)
</code>
        </td>
        </tr>
        <tr>
            <td>
                <strong>Pros:</strong>
                <ul>
                    <li> Save some future's troubles. </li>
                    <li> Can be easier to design different parts of the program. </li>
                </ul>
            </td>
            <td>
                <strong>Pros:</strong>
                <ul>
                    <li> Save memory. </li>
                </ul>
            </td>
        </tr>
        <tr>
            <td>
                <strong>Cons:</strong>
                <ul>
                    <li> Cost more memory. </li>
                </ul>
            </td>
            <td>
                <strong>Cons:</strong>
                <ul>
                    <li> Potentially contain a critical bug that needs handling more elegantly. </li>
                </ul>
            </td>
        </tr>
    </table>
</details>

### :heavy_check_mark: **Methods**
```python
list_name.method_name()
```
**Classes** which we'll learn in [Python for AI 105](/_posts/2025-06-18-python105.md) can be used to implement data structures. The class that carries out list comprises certain methods, so lists can access all of them. Some methods update a list when using them, which helps save memory by avoiding the need to assign a new variable. 

We'll use `a_list = [4, 5, 6, 7, 4, 7]` for examples below.

| Methods | Purposes | Returned Value | List Updated | Examples |
|:-------:|----------|:--------------:|:------------:|----------|
| **append()**  | Add a given value   to the end                             | None     | :white_check_mark: | `a_list.append(10)` <br> `a_list` <br> `>>> [4, 5, 6, 7, 4, 7, 10]` |
| **insert()**  | Add a given value   to the position of a given index       | None     | :white_check_mark: |`a_list.insert(2,10)` <br> `a_list` <br> `>>> [4, 5, 10, 6, 7, 4, 7]` |
| **extend()**  | Concatenate a given list to the original                   | None     | :white_check_mark: |`a_list.extend([10, 8])` <br> `a_list` <br> `>>> [4, 5, 6, 7, 4, 7, 10, 8]` |
| **pop()**     | Delete the last element by default or delete by index      | Value    | :white_check_mark: |`a_list.pop(1)` <br> `>>> 5` <br> `a_list` <br> `>>> [4, 6, 7, 4, 7]` |
| **remove()**  | Delete the first element of a given value                  | None     | :white_check_mark: |`a_list.remove(7)` <br> `a_list` <br> `>>> [4, 5, 6, 4, 7]` |
| **del**       | Delete a given list of elements                            | N/A      | :white_check_mark: |`del a_list[1:4]` <br> `a_list` <br> `>>> [4, 7, 7]` |
| **clear()**   | Delete all elements                                        | None     | :white_check_mark: |`a_list.clear()` <br> `a_list` <br> `>>> []` |
| **index()**   | Return the index of the first occurence of a given value   | Index    | :x:                |`a_list.index(7) >>> 3` <br> `a_list` <br> `>>> [4, 5, 6, 7, 4, 7]` |
| **reverse()** | Reverse elements                                           | None     | :white_check_mark: |`a_list.reverse()` <br> `a_list` <br> `>>> [7, 4, 7, 6, 5, 4]` |
| **sort()**    | Arrange elements in an increasing order by default         | None     | :white_check_mark: |`a_list.sort()` <br> `a_list` <br> `>>> [4, 4, 5, 6, 7, 7]` |
| **count()**   | Return the number of a given value                         | Number of times an element appears | :x:                |`a_list.count(7)` <br> `>>> 2` <br> `a_list` <br> `  >>> [4, 5, 6, 7, 4, 7]` |
| **copy()**    | Make a copy of the list                                    | New list that is the same as the original | :x:                |`a_list.copy()` <br> `>>> [4, 5, 6, 7, 4, 7]` <br> `a_list` <br> `>>> [4, 5, 6, 7, 4, 7]` |

<br>

:pushpin: **Pro tip:** Avoid using built-in methods. Instead, build them.

:brain: In fact, we can actually write our own functions that replicate the behavior of these methods using only basic concepts we have learned so far. 

<details class = "bordered-block">
    <summary>
        <strong>Practice Exercises</strong>
    </summary>
    <div class = "inside-bordered-block language-python">
        <strong>Exercise 1: I write the funciton below to insert a given value to a list at a given index. Will my code work as expected? Explain why or why not. If it doesn't, suggest at least one solution to fix it.</strong>
        <code class = "multi-line-code">
            def my_insert(data, index, value):
                length = len(data)
                result = []
                for i in range(length):
                    if i == index:
                        data.append(value)
                        data.append(data[i])
                    data.append(data[i])
                return result
        </code> <br>
        <strong>Exercise 2: Write a different function that acts like <code>insert()</code>.</strong>
        <br>
        <strong>Exercise 3: Write functions for other methods if possible.</strong> 
        <br>
        <strong>Exercise 4: <code>remove()</code> only deletes the first occurence of a given value. Can you write a function that deletes all the occurences of a given value? If yes, write the function. Or else, explain why not.</strong> 
    </div>
</details>

:warning: data.copy() and assignment

<br>

### :heavy_check_mark: **Built-in Functions**

| Functions | Purposes | Returned Value | List Updated | Examples |
|:---------:|----------|:--------------:|:------------:|----------|
| **len()**       | Return the number of elements                                                 | Quantity                  | :x: | `len(a_list)` <br> `>>> 6` |
| **min()**       | Return the minimum element                                                    | Quantity                  | :x: | `min(a_list)` <br> `>>> 4` |
| **max()**       | Return the maximum element                                                    | Quantity                  | :x: | `max(a_list)` <br> `>>> 7` |
| **sorted()**    | Arrange elements in an ascending order by default                             | New List                  | :x: | `sorted_a_list = sorted(a_list)` <br> `sorted_a_list` <br> `>>> [4, 4, 5, 6, 7, 7]` |
| **sum()**       | Return the sum of all elements                                                | Quantity                  | :x: | `sum(a_list)` <br> `>>> 33` |
| **reversed()**  | Reverse elements                                                              | A reverse iterator object | :x: |   |
| **enumerate()** | Return a sequence of tuples of matching index and value                       | An enumerate object       | :x: |   |
| **zip()**       | Take two lists and return an iterator of tuples of values with matching index | A zip object              | :x: |   |



In addition to their values, each element is associated to an index which is automatically created when the list is defined. Forward indices start at 0 for the first element on the left and increase one by one till the end of the list. In practice, you get to elements' values via their indices. Let's take a look at an example. 
```python
    # create a list of fruits
    fruits = ["apple", "orange", "banana", "grape", "mango"]

    fruits[0]
    >>> "apple"
    fruits[2]
    >>> "banana"

    '''
    +-------+-------+--------+--------+-------+-------+
    | Fruit | apple | orange | banana | grape | mango |
    +=======+=======+========+========+=======+=======+
    | Index |   0   |   1    |   2    |   3   |   4   |
    +-------+-------+--------+--------+-------+-------+

    +---------+-------+
    | Fruit   | Index |
    +=========+=======+
    | "apple" |   0   |
    +---------+-------+
    | "orange"|   1   |
    +---------+-------+
    | "banana"|   2   |
    +---------+-------+
    | "grapes"|   3   |
    +---------+-------+
    | "mango" |   4   |
    +---------+-------+

    - 0 → apple 🍎
    - 1 → orange 🍊
    - 2 → banana 🍌
    - 3 → grape 🍇
    - 4 → mango 🥭
    '''
```
~~DEMONSTRATION~~

Of course, elements of a list can be logic, numbers, and more.





If you tried some Python exercises, you probably have encountered some of the errors we'll mention shortly. 


<hr>

## :mag_right: **2D List**


<details class = "bordered-block">
    <summary>
        <strong>Practice Exercises</strong>
    </summary>
    <table class = "language-python">
        <tr>
            <th>Immutable</th>
            <th>Mutable</th>
        </tr>
        <tr>
        <td>
<code class = "multi-line-code">
    def 
</code>
        </td>
        <td>
<code class = "multi-line-code">
    def 
</code>
        </td>
        </tr>
    </table>
</details>


<details class = "bordered-block">
    <summary>
        <strong>Practice Exercises</strong>
    </summary>
    <div class = "inside-bordered-block language-python">
        <strong>Exercise 1: Do these for loops give the same result? Explain why or why not.</strong>
        <code class = "multi-line-code">for x in range(7):
                print(x)
                if x == 5:
                    break
            print("iloveaivietnam")
        </code>
        <code class = "multi-line-code">for x in range(7):
                if x == 5:
                    break
                print(x)
            print("iloveaivietnam")
        </code> <br>
        <strong>Exercise 2: Understand this while loop and modify itp to see how many times you need to roll a dice to get a six.</strong>
        <code class = "multi-line-code">import random
            while True:
                roll = random.randint(1, 6)
                print(f"You rolled {roll}.)
                if roll == 6:
                    print("You win!")
                    break
        </code> <br>
        <strong>Exercise 3: Give an example where you can replace a for loop with break with a while loop?</strong> 
        <br>
        <strong>Exercise 4: In which case do you think you have to use while loop with break instead of whle loop? Can you give an example?</strong>
    </div>
</details>


<hr>

## :mag_right: **Examples and Exercises**

<details class = "bordered-block">
    <summary>
        <strong>Practice Exercises</strong>
    </summary>
    <div class = "inside-bordered-block language-python">
        <strong>Exercise 1: PI Estimation (Gregory-Leibniz Series)</strong>
        $$\text{PI} \approx 4 \sum_{i=1}^{n} \frac{(-1)^{i+1}}{2i-1}$$ <br>
        <strong>Exercise 2: PI Estimation (Nilakantha Series)</strong>
        $$\text{PI} \approx 3 + 4 \sum_{i=0}^{n} \frac{(-1)^{i}}{(2i+2)(2i+3)(2i+4)}$$ <br>
        <strong>Exercise 3: Quadratic Root (Newton Method)</strong> <br>
        The Newton Method can be used to approximate a root of $f(x) = 0$ by the general formula 
        $$x_{n+1} = x_{n} - \frac{f(x_n)}{f'(x_{n})}.$$
        Generate a mathematical formula for $f(x) = x^2 - a$ to approximate $\sqrt{a}$. Rewrite the formula in a programming fashion. Write a function that $a$ and $n$ and returns $\sqrt{a}$. 
        <br>
        <strong>Exercise 4: Write a function the compute the factorial of a positive number using a loop.</strong>
        <br>
        <strong>Exercise 5: Write a function the compute the factorial of a non-negative number using a loop.</strong>
        <br>
        <strong>Exercise 6: Give possible mistakes one might make when writing a factorial function.</strong>
    </div>
</details>

<hr>

## :speech_balloon: **FAQ**
Here are some good questions asked during class. The answers to these questions came from either some admin, TA, STA, or members of AIO2025.

| Questions | Answers |
|-----------|---------|
| What is an embedded system? | No. Python reads code top-down. |
| Can I write 2 functions A and B where A calls B and B calls A? Why does it give an error when I refer to one before defining it? | Yes. Such a situation is called **mutual recursion**. Python doesn't execute function definitions which consist of function references. It only starts evaluating when a function is called. |

<br>

#### :clipboard: Check List

- [x] Exercise 1 (date)
- [ ] Exercise 2 (date)
  - [x] Put on left sock
  - [ ] Put on right sock
- [x] Go to school
