---
layout: post
title: Python for AI 103
date: 2025-06-15
description: list
tags: coding AIO basic-math algorithm
categories: AI
giscus_comments: true 
pretty_table: true
toc:
    sidebar: right
---

**Acknowledgement:** This post is based mainly on Dr. Quang-Vinh Dinh's presentation and slides and partly on on TA, STA, and peers' responses from the course, AIO2025, offered by AI VIETNAM.

**Relevance:** The audience of this lecture were those who learned [Python for AI 102](/_posts/2025-06-06-python102.md), have some coding experience, and know a bit of AI knowledge. It is recommended that complete beginners should read the materials and spend time practicing writing code without the assistance of AI over and over again to improve their coding and code-reading skills. Note that only necessary Python knowledge is introduced here.

> Think about anything you learn as a game. A game always has rules. We just need to follow the rules to solves problems and conquer the game.
> When learning programming, don't trust anyone or any book. Only be convinced that a program is correct once you have already tried it yourself, seen the expected result, and explore the reasoning behind the answer through questioning. <br>
> -Dr. Quang-Vinh Dinh

In today's post, we'll delve into list in Python. We won't approach it like computer scientists where they actually know how to integrate it into embedded systems so that physical devices can perform specific tasks. Instead, we'll approach it from a programming viewpoint.

<hr>

:pushpin: **Pro tip:** When learning a new concept in programming, read documentation and try out different kinds of examples to truly understand it. 

### **Data Structure**
Data structures were created in order for computers to handle data faster. Such speed is not so significant in practice these days since computers' hardware is upgraded every so often.

A data struture is a format used to organize and store data for efficient data access and management. It also reflects on real life situations, for instance, making a dictionary, a customer lineup scenario, or constructing a family tree. 

There are two types of data structures and they are arranged according to their usage frequency from top to bottom. 

| Built-in   | User-defined |
|:----------:|:------------:|
| List       | Stack        |
| Dictionary | Queue        |
| Tuple      | Tree         |
| Set        | Graph        |
|            | Linked List  |

<hr>

## **1D List**
```python
list_name = [element_1, ..., element_n]
```
A list is a container of elements. As mentioned before a list can consist of different kinds of data representation, but we use just one type for each list in practice, especially numbers since computers only works with them.
```python
list_0 = [] # an empty list
list_00 = list() # an empty list
list_1 = [7, -5, 8]
list_2 = [5, 4.7, -2, "hello", 0, True, -1000.1, 5, 5, 5]
list_3 = ["aivietnam", [2, -3, 0, 5]]
```
The rules for list are:
- All elements are placed inside a pair of brackets.
- Every two consecutive ones are separated by a comma.
- As soon as a list is created, so are its indices.
    - :arrow_right: **Forward index:** starts at $0$ and increases by $1$
    - :arrow_left: **Backward index:** starts at $-1$ and decreased by $1$.
    - `list_name[index]` is used to access an element with `index` is less than the number of elements. 

We mainly use forward index and maybe, when retrieving the last element, $-1$ from backward index.
```python
list_1 = [7, -5, 8] 
list_1[0]
>>> 7
list_1[-1]
>>> 8
list_1[9]
>>> IndexError
```
There are more rules in this list game coming up now.
### **Operators**
#### **Updating an Element**
Lists are **mutable**, meaning their content can be changed after creation. Mutable and immutable are coming soon in this post. 
```python
list_1 = [7, -5, 8] 
list_1[2] = 10000
list_1
>>> [7, -5, 10000]
list_0 = []
list_0[1] = 5
>>> IndexError
```

#### **Slicing**
Similar to the **range(start, stop, step)** function, the **slicing** syntax of list is `list[start:end:step]`.
- *start*, *end*, and *step* are indices. Their values are all optional. 
- *start* is $0$ and *step* is $1$ by default.
- That *end* is not specified means until the end on either side depending on *step*.

```python
a_list = [3, 4, 5, 6, 7, 8, 9, 10]
a_list[:3]
>>> [3, 4, 5]
a_list[6:]
>>> [9, 10]
a_list[0:1000:3]
>>> [4, 7, 10]
a_list[::-1]
>>> [10, 9, 8, 7, 6, 5, 4, 3]
a_list[3:1]
>>> [] 
```

<details class = "bordered-block">
    <summary>
        <strong>Practice Exercises</strong>
    </summary>
    <div class = "bordered-inner-block language-python">
        Given the list <code>animal = ['e', 'l', 'e', 'p', 'h', 'a', 'n', 't']</code>. Use slicing in different ways if possible to answer the following.
        <ol>
            <li> Get the first four letters. </li>
            <li> Get the last three letters. </li>
            <li> Get the every second letter starting from index 1. </li>
            <li> Get the all letters except the first and last. </li>
            <li> Get the middle three letters. </li>
            <li> Get an empty list. </li>
            <li> Reverse the letters. </li>
            <li> Get the letters h, p, e . </li>
            <li> What does <code>animal[1:-1:2]</code> return? </li> 
        </ol>  
    </div>
</details>

#### **$+$ and $*$ Operators**
The $+$ operator is to concatenate lists. The $*$ operator is to make multiple copies of a list and concatenate them one by one. The original list remains unchanges unless it is assigned to the addition or multiplication.
```python
list_1 = [7, -5, 8]
list_2 = [5, 4.7, -2, "hello", 0, True, -1000.1]
addition = list_1 + list_2
addition
>>> [7, -5, 8, 5, 4.7, -2, "hello", 0, True, -1000.1, 5, 5, 5]
multiplication = list_1 * 3
multiplication
>>> [7, -5, 8, 7, -5, 8, 7, -5, 8]
```
The $*$ operator can be used when you want to initialize a list with some default value. For example, a teacher is making a file to input students' quiz grades later. They will do `quizzes = [-1] * 50` because they can avoid typing one number too many times and ensure a student's grade will not have been put in yet as they see the number. 

### **Mutable and Immutable**
<details class = "bordered-block">
    <summary>
        <strong>Practice Exercises</strong>
    </summary>
    Point out the differences between the functions below. Is the original list updated in both? Explain why or why not. How is it related to immutable and mutable here? What are pros and cons of each? In which cases should we use them? 
    <table class = "language-python">
        <tr>
            <th>Immutable</th>
            <th>Mutable</th>
        </tr>
        <tr>
            <td>
<pre>
<code class = "language-python">
def square(data):
    result = []
    for value in data:
        result.append(value * value)
    return result
# test
data = [6, 5, 7, 1, 9, 2]
print(data)
data_s = square(data)
print(data_s)
</code>
</pre>
            </td>
            <td>
<pre>
<code class = "language-python">
def square(data):
    length = len(data)
    for i in length:
        value = data[i] * data[i]
        data[i] = value
# test
data = [6, 5, 7, 1, 9, 2]
print(data)
data_s = square(data)
print(data_s)
</code>
</pre>
            </td>
        </tr>
        <tr>
            <td>
                <strong>Pros:</strong>
                <ul>
                    <li> Save some future's troubles. </li>
                    <li> Can be easier to design different parts of the program. </li>
                </ul>
            </td>
            <td>
                <strong>Pros:</strong>
                <ul>
                    <li> Save memory. </li>
                </ul>
            </td>
        </tr>
        <tr>
            <td>
                <strong>Cons:</strong>
                <ul>
                    <li> Cost more memory. </li>
                </ul>
            </td>
            <td>
                <strong>Cons:</strong>
                <ul>
                    <li> Potentially contain a critical bug that needs handling more elegantly. </li>
                </ul>
            </td>
        </tr>
    </table>
</details>

<br>
### **Methods**
```python
list_name.method_name()
```
**Classes** which we'll learn in [Python for AI 105](/_posts/2025-06-18-python105.md) can be used to implement data structures. The class that carries out list comprises certain methods, so lists can access all of them. Some methods update a list when using them, which helps save memory by avoiding the need to assign a new variable. 

We'll use `a_list = [4, 5, 6, 7, 4, 7]` for examples below.

| Methods | Purposes | Returned Value | List Updated | Examples |
|:-------:|----------|:--------------:|:------------:|----------|
| **append()**  | Add a given value   to the end                             | None     | :white_check_mark: | `a_list.append(10)` <br> `a_list` <br> `>>> [4, 5, 6, 7, 4, 7, 10]` |
| **insert()**  | Add a given value   to the position of a given index       | None     | :white_check_mark: |`a_list.insert(2,10)` <br> `a_list` <br> `>>> [4, 5, 10, 6, 7, 4, 7]` |
| **extend()**  | Concatenate a given list to the original                   | None     | :white_check_mark: |`a_list.extend([10, 8])` <br> `a_list` <br> `>>> [4, 5, 6, 7, 4, 7, 10, 8]` |
| **pop()**     | Delete the last element by default or delete by index      | Value    | :white_check_mark: |`a_list.pop(1)` <br> `>>> 5` <br> `a_list` <br> `>>> [4, 6, 7, 4, 7]` |
| **remove()**  | Delete the first element of a given value                  | None     | :white_check_mark: |`a_list.remove(7)` <br> `a_list` <br> `>>> [4, 5, 6, 4, 7]` |
| **del**       | Delete a given list of elements                            | N/A      | :white_check_mark: |`del a_list[1:4]` <br> `a_list` <br> `>>> [4, 7, 7]` |
| **clear()**   | Delete all elements                                        | None     | :white_check_mark: |`a_list.clear()` <br> `a_list` <br> `>>> []` |
| **index()**   | Return the index of the first occurence of a given value   | Index    | :x:                |`a_list.index(7) >>> 3` <br> `a_list` <br> `>>> [4, 5, 6, 7, 4, 7]` |
| **reverse()** | Reverse elements                                           | None     | :white_check_mark: |`a_list.reverse()` <br> `a_list` <br> `>>> [7, 4, 7, 6, 5, 4]` |
| **sort()**    | Arrange elements in an increasing order by default         | None     | :white_check_mark: |`a_list.sort()` <br> `a_list` <br> `>>> [4, 4, 5, 6, 7, 7]` |
| **count()**   | Return the number of a given value                         | Number of times an element appears | :x:                |`a_list.count(7)` <br> `>>> 2` <br> `a_list` <br> `  >>> [4, 5, 6, 7, 4, 7]` |
| **copy()**    | Make a copy of the list                                    | New list that has the same value as the original | :x:                |`a_list.copy()` <br> `>>> [4, 5, 6, 7, 4, 7]` <br> `a_list` <br> `>>> [4, 5, 6, 7, 4, 7]` |

<br>

:warning: `list_name.copy()` and assigning one list to another does different things behind the scene.

<table class = "language-python">
    <tr>
        <th>Tasks</th>
        <th>copy() Method</th>
        <th>Assignment (=)</th>
    </tr>
    <tr>
        <td><strong>Code</strong></td>
        <td>
<pre>
<code class = "language-python">
original_data = [1, 2]
copied_data = original_data.copy()
copied_data[0] = 9
print(original_data)
print(copied_data)
</code>
</pre>
        </td>
        <td>
<pre>
<code class = "language-python">
original_data = [1, 2]
copied_data = original_data
copied_data[0] = 9
print(original_data)
print(copied_data)
</code>
</pre>
        </td>
    </tr>
    <tr>
        <td><strong>Rendered output</strong></td>
        <td>
<pre>
<code class = "language-python">
[1, 2]
[9, 2]
</code>
</pre>       
        </td>
        <td>
<pre>
<code class = "language-python">
[9, 2]
[9, 2]
</code>
</pre>
        </td>
    </tr>
    <tr>
        <td><strong>Explanation</strong></td>
        <td>
            <ul>
                <li> It's like two bags, one brand name (<code>original_data</code>) and one replica (<code>copied_data</code>). </li>
                <li> They look the same but how one is used has no effect on the other.  </li>
                <li> Technically, <code>copy()</code> creates a new list object which lives in a different memory address from the existing list. </li>
            </ul>
        </td>
        <td>
            <ul>
                <li> It's like the same person with a real name (<code>original_data</code>) and a nick name (<code>copied_data</code>). </li>
                <li> <code>copied_data</code> does whatever <code>original_data</code> does and vice versa. </li>
                <li> Technically, assignment creates a reference to the existing variable, allowing both to point to the same list object in memory. </li>
            </ul>
        </td>
    </tr>
</table>

<br>
:pushpin: **Pro tip:** For newbies, avoid using built-in methods. Instead, build them.

In fact, we can actually write our own functions that replicate the behavior of these methods using only basic concepts we have learned so far. 

<details class = "bordered-block">
    <summary>
        <strong>Practice Exercises</strong>
    </summary>
    <div class = "bordered-inner-block language-python">
        <strong>Exercise 1:</strong> I write the funciton below to insert a given value to a list at a given index. Will my code work as expected? Explain why or why not. If it doesn't, suggest at least one solution to fix it.
<pre>
<code class = "language-python">
def my_insert(data, index, value):
    length = len(data)
    result = []
    for i in range(length):
        if i == index:
            data.append(value)
            data.append(data[i])
        data.append(data[i])
    return result
</code>
</pre>
        <strong>Exercise 2:</strong> Write different functions that act like <code>insert()</code>, at least one with returning a value and one without.
        <br>
        <strong>Exercise 3:</strong> Write functions for other methods if possible.
        <br>
        <strong>Exercise 4:</strong> <code>remove()</code> only deletes the first occurence of a given value. Can you write a function that deletes all the occurences of a given value? If yes, write the function. Or else, explain why not.
    </div>
</details>

<br>
### **Built-in Functions**

| Functions | Purposes | Returned Value | List Updated | Examples |
|:---------:|----------|:--------------:|:------------:|----------|
| **len()**       | Return the number of elements                                                 | Quantity                  | :x: | `len(a_list)` <br> `>>> 6` |
| **min()**       | Return the minimum element                                                    | Quantity                  | :x: | `min(a_list)` <br> `>>> 4` |
| **max()**       | Return the maximum element                                                    | Quantity                  | :x: | `max(a_list)` <br> `>>> 7` |
| **sorted()**    | Arrange elements in an ascending order by default                             | New List                  | :x: | `sorted_a_list = sorted(a_list)` <br> `sorted_a_list` <br> `>>> [4, 4, 5, 6, 7, 7]` |
| **sum()**       | Return the sum of all elements                                                | Quantity                  | :x: | `sum(a_list)` <br> `>>> 33` |
| **reversed()**  | Reverse elements                                                              | A reverse iterator object | :x: |   |
| **enumerate()** | Return a sequence of tuples of matching index and value                       | An enumerate object       | :x: |   |
| **zip()**       | Take two lists and return an iterator of tuples of values with matching index | A zip object              | :x: |   |

<br>
**enumerate()** replaces regular for loop method to access indices and values in a list, speeding up the coding process.
```python
data_0 = [4, 5, 6]

# print index and value in pairs using enumerate()
for index, value in enumerate(data_0):
    print(index, value)

# The rendered output is
0 4
1 5
2 6
```

**zip()** acts like a zipper that interlocks two rows of teeth. Difference in length of the rows doesn't matter; it can zip to the end of the shorter one. We use this method when we want to handle data in pairs.
```python
data_1 = [1, 2, 3]
data_2 = ['john', 'mary', 'peter']

# print in pairs using zip()
for position, name in zip(data_1, data_2):
    print(v1, v2)

# The rendered output is
1 john
2 mary
3 peter
```

<details class = "bordered-block">
    <summary>
        <strong>Practice Exercises</strong>
    </summary>
    <div class = "bordered-inner-block language-python">
        <strong>Exercise 1:</strong> Write functions for other built-in functions if possible.
        <br>
        <strong>Exercise 2:</strong> Write a function that sums up even and odd numbers.
        <br>
        <strong>Exercise 3:</strong> Write a function that sums up elements with even and odd indices.
    </div>
</details>

<br>
### **List Comprehension**
```python
list_name = [condition_to_branch_x for x in data condition_to_filter_x]
```
List comprehension is popular among programmers, allowing us to write a simple for loop in only one line. There are three parts in list comprehension. 

:question: You may ask: **Do I need all of them for list comprehension? Am I reading this from left to right or what?** 

Let me answer you one by one. The short answer for the first one is no; we'll discuss it right below. For the second question, well yeah if you read it as an English text block. But, it's Python language here. We should *read* list comprehension in the following order. 
1. Looping `for x in data`.
2. Filtering `condition_to_filter_x`.
3. Routing `condition_to_branch_x`.

Imagine a company is hiring summer interns using a list of job-seekers. They need to evaluate all of them one by one. They can either accept all the candidates or filter some out.

#### **(Optional) condition_to_filter**
```python
if x_filter_condition
```
This condition can have `if` only. Here, the company decides to be selective, setting up an interview for each applicant. If someone passes the interview, they proceed with the internship. Otherwise, they are out. 

#### **(Required) condition_to_branch_x**
```python
[if_value if x_if_condition else else_value for x in data]

[if_value if x_if_condition else elif_value if x_elif_condition else else_value for x in data]
```
The condition must be either `if-else` or `if-elif-else`. For the latter, it's written as nested `if-else`. A syntax error is raised if `else` is missing.

Think back about the supposed scenario. All the candidates are joining the internship of the company at this point. Now the supervisor must assign jobs to the interns based on their qualifications. Therefore, it is logical to have `if-else` or `if-elif-else` in this part.

Besides, that there is no `else` means even though an incoming intern has passed the whole hiring process, they must still be qualified for something else to be hired. What about those who cannot meet that "something else"? This makes no sense. 

<details class = "bordered-block">
    <summary>
        <strong>Practice Exercises</strong>
    </summary>
    <div class = "bordered-inner-block language-python">
        <strong>Exercise 1:</strong> What is the output of this program? Give your reasoning.
<pre>
<code class = "language-python">
data = [1, 2]
for x in data:
    x = 20
print(f"data is {data}")
</code>
</pre>
        <strong>Exercise 2:</strong>  What is the output for <code>a_list = [4, 5, 6, 7, 4, 7]</code>?
        <ol>
            <li> a_list.sort(rever = True) </li>
            <li> sorted(a_list, reverse = True) </li>
        </ol>
        <strong>Exercise 3:</strong>  Write a for loop that squares all elements of a list. Also write it with list comprehension.
        <br>
        <strong>Exercise 4:</strong>  Write the following [activation functions](/_posts/2025-06-08-python-exercises-activation-funtions.md) for lists using list comprehension. Write helper functions when needed.
        <ol>
            <li> Sigmoid function </li>
            <li> ReLU function </li>
        </ol>
        <strong>Exercise 5:</strong>  What is the output of <code>print(data_a)</code> for each list comprehension line below given <code>data = [1, 5, -4, 3, -2]</code>? Explain your answer.
<pre>
<code class = "language-python">
data_a = [x if x > 0 else 0 for x in data]
data_a = [x if x > 0 for x in data]
data_a = [x for x in data if x > 0]
data_a = [x for x in data if x > 0 else 0]
</code>
</pre>
    </div>
</details>

<hr>

## **2D List**
Real-world data extensively need more-than-one-dimension structure/matrix to represent it. For instance, color image data (at least 3D), gray scale image data (2D), text data (2D). Later on, we'll use a package called **numpy** to this matter but use list for now. We'll focus on two dimension here. 

To describe a **2D matrix**, we can design a **nested list** which is a list with each element being a list of the same size. Take `data_2d = [[1, 2, 3], [4, 5, 6]]`.

| Plain Language | Python Language |
|----------------|-----------------|
| data_2d is a box | data_2d points to a list object |
| the box holds 2 name tags | a list object contains two pointers |
| the first tag says "look over here", which directs to a baby box named [1, 2, 3] | the element of index 0 points to another list object with value [1, 2, 3] |
| the second tag says "look over there", which directs to another baby box named [4, 5, 6] | the element of index 1 points to another list object with value [4, 5, 6] |

<br>
Since each element of a nested 2D list is a list itself, you can access the elements of this inner list just like generally accessing elements of a list. For example, `data[1][2] = 6`.

If you want to think about a nested 2D list as a matrix, you can think that each element is a row and each column is formed from items of the same index in baby boxes. As such, the number of rows is that of outer elements and the number of columns is that of inner items in each baby box. In this case, this matrix has 2 rows and 3 columns.
```python
data_2d = [[1, 2, 3], [4, 5, 6]]
num_rows = len(data_2d)
num_cols = len(data_2d[0])
# iterate through a nested list using a nested loop and print the matrix
for i in range(num_rows):
    for j in range(num_cols):
        # print each row
        print(data_2d[i][j], end = ' ')
    print()

'''
The rendered output is
1 2 3
4 5 6
'''
```

<details class = "bordered-block">
    <summary>
        <strong>Practice Exercises</strong>
    </summary>
    <div class = "bordered-inner-block language-python">
        <strong>Exercise 1:</strong> Write a function that prints a 2D matrix given a list, the number of rows, and the number of columns with and without returning a value.
        <br>
        <strong>Exercise 2:</strong> Use exercise 1 to write a function that prints a 2D matrix whose elements are square of elements of a nested list with or without returning a value.
        <ol>
            <li> a_list.sort(reverse = True) </li>
            <li> sorted(a_list, reverse = True) </li>
        </ol>
        <strong>Exercise 3:</strong> Write a function that performs <strong>Hadamard Product (Element-wise Multiplication)</strong>
        <br>
        <strong>Exercise 4:</strong> Use exercise 1, what is the output?
<pre>
<code class = "language-python">
width = 2
height = 3
matrix = [[0, 0], [0, 0], [0, 0]]
for i in range(height):
    for j in range(width):
        matrix[i][j] = i * width + j
print_matrix(matrix)
</code>
</pre>
<pre>
<code class = "language-python">
width = 3
height = 3
matrix = [[1, 1, 1], [1, 0, 0], [1, 0, 0]]
for i in range(1, height):
    for j in range(1, width):
        matrix[i][j] = matri[i-1][j] + matrix[i][j-1] + matrix[i-1][j - 1]
print_matrix(matrix)
</code>
</pre>
    </div>
</details>

<hr>

## **Algorithm and Case Study**

<details class = "bordered-block">
    <summary>
        <strong>Practice Exercise: Linear Searching Algorithm on List</strong>
    </summary>
    <div class = "bordered-inner-block language-python">
        <strong>Exercise 1:</strong>  Write a function that mimics <code>index()</code> using <code>enumerate()</code>.
        <br>
        <strong>Exercise 2:</strong>  Write a function that mimics <code>sort()</code> using <code>min()</code>, <code>remove()</code>, <code>append()</code>.
    </div>
</details>

<details class = "bordered-block">
    <summary>
        <strong>Case Study: List and Integral</strong>
    </summary>
    <div class = "bordered-inner-block language-python">
        integral from -inf to inf
        integral from inf to a -> approx summation of area of rectangles like we learn before -> equal as deltax goes to 0 -> rules f(a), f(b), and f(b)-f(a) because of non-intersection/overlap part
        as we find c very close to a then F(c)=integral -inf to a (fxdx)+ fcdx = F(a)+fcdx
        application: in image data as a matrix, want to compute average, similarly sum of elements of a submatrix. instead of using accumulation patter, use integral
        in this case, he use 1d matrix for convenience. 
        F(x)=F(x-1)+f(x) -> bigO is O(1) assuming we don't count computation of F(x-1) in -> appliled in controlling system?
        in image data, still count that in, but not cost a lot
        when compute summation of a submatrix, we just need two positions to complete the task assuming we have big F already instead of running through the whole submatrix and accummulate 
        this problem is called integral image, face detection, cvpr
        what about 2d?? ad Vinh said there will be material
    </div>
</details>

<hr>

## :speech_balloon: **FAQ**
Here are some good questions asked during class. The answers to these questions came from either some admin, TA, STA, or members of AIO2025.

| Questions | Answers |
|-----------|---------|
| What is an embedded system? | No. Python reads code top-down. |
| Can I write 2 functions A and B where A calls B and B calls A? Why does it give an error when I refer to one before defining it? | Yes. Such a situation is called **mutual recursion**. Python doesn't execute function definitions which consist of function references. It only starts evaluating when a function is called. |
| A resource for mutable and immutable. | <https://www.geeksforgeeks.org/mutable-vs-immutable-objects-in-python/> |
| A resource for pass by reference and pass by value | <https://www.tutorialspoint.com/python/python_functions.htm> |
| How do I tell when two variables point to the same object in memory? | You either need to understand what's going on very well or check with the `id()` function. Each object has one and only one identity. |
| Can I use list comprehension to create a completely new list? | Yes. It all depends on how you choose your values inside the branch condition. |
| Visualization for Sorted List | <https://www.cs.usfca.edu/~galles/visualization/Search.html> |
| Are integral image and box filtering similar? | Yes. Box filtering is in graphics. |



copy() và deepcopy() là gì vậy ạ? Khi nào thì nên sử dung phương thức nào ạ? - Theo mình hiểu thì, copy() sẽ tạo một đối tượng mới ở cấp ngoài cùng, những sẽ dùng chung các phần tử bên trong với nhau. Còn deepcopy() sẽ tạo một đối tượng mới hoàn toàn và độc lập.

<br>

<https://colab.research.google.com/drive/14sg8E-rfZ6JwIOD-i6Y6BFHvhmPk0VHc?usp=sharing#scrollTo=pl_mAkxliU2q&uniqifier=1>

<br>

#### Check List

- [x] Exercise 1 (date)
- [ ] Exercise 2 (date)
  - [x] Put on left sock
  - [ ] Put on right sock
- [x] Go to school
