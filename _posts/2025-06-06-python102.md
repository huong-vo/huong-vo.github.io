---
layout: post
title: Python for AI 102
date: 2025-06-14
description: basic errors and loops
tags: coding AIO basic-math
categories: AI
giscus_comments: true 
pretty_table: true
toc:
    sidebar: right
---

:round_pushpin: **Acknowledgement:** This post is based mainly on Dr. Quang-Vinh Dinh's presentation and slides and partly on peers' responses from the course, AIO2025, offered by AI VIETNAM.

:thought_balloon: **Relevant information and opinions:** The audience of this lecture were those who learned [Python for AI 101](/_posts/2025-06-12-python101.md), have some coding experience, and know a bit of AI knowledge. It is reccommended that complete newbies should read the materials and spend time practicing writing code without the assistance of AI over and over again to improve their coding and code-reading skills. Note that only necessary Python knowledge is introduced here.

> When you get a result that is not what you expect, do not ask for help immediately. Instead, run the code by hand to figure out where your logic breaks, modify the program, and run it again. Repeat this process and your skills will be through the roof at breakneck speed. <br>
> -Dr. Quang-Vinh Dinh

In today's post, we'll look at common errors and loops in Python. 

<hr>

## :mag_right: **Common Errors**
If you tried some Python exercises, you probably have encountered some of the errors we'll mention shortly. 

### :one: **NameError**
NameError occurs when you try to use a variable or function that hasn't been defined. 
```python
a = 7 
Print(a)

c = a + b
print(c)

def a_function(x): 
    a_variable = 4
    return x * a_variable

print(a_variable)
```

### :two: **SyntaxError**


### :three: **ZeroDivisorError**
```python
a = 7
b = 0
c = a / b
print(c)
```

### :four: **TypeError**

### :five: **IndexError**
```python
a_list = [1, "love", "aivietnam"]
print(a_list[3])
```


### :six: **IndentationError** 
```python
a = 7 
    b = 8

print(b)
```

### :seven: **ValueError**

### :eight: **ModuleNotFoundError**
```python
import mymodule

print("iloveaivietnam")
```

### :nine: **RecursionError**
```python
def a_function(n):
    return a_function(n)

a_function(7)
```


~~exercise~~
<details class = "bordered-block">
<summary>
    <strong>Practice Exercises</strong>
</summary>
<pre>
<code class = "inside-bordered-block language-python">
# Exercise 1
numbers = list(range(10))
for i in numbers:
    if numbers[i] == 5:
        numbers.remove(i)
<hr># Exercise 2
</code>
</pre>
</details>

~~TRY-EXCEPT~~At least one of us entered our email in a wrong form to an app or a website once. In such a situation, you usally 

<hr>

Suppose you want to draw a million stars. It's take forever to complete this task unless you, I guess, desire to be a professional star drawer. There must be a way to **avoid such an endlessly repetitive action**. Exactly, it's a loop, which repeats some action within itself. In Python, there are two types of loops including `for` and `while` loops.

## :mag_right: **For Loop**
```python
# code before for

for element in iterable:
    # code inside for

# code after for
```
- A `for` loop is used when the number of times you want to *iterate* over a sequence (a.k.a, an *iterable*).
- An *iterable* can be a string, list, dictionary, tuple, and range().
- An *element*'s name can be almost anything but is usually `i` or `_`. 

For now, we'll focus on the **range(start, stop, step)** function. 
- *start* is $0$ and *step* is $1$ by default. Passing arguments to these parameters is optional.
- The *stop* value is required. 
- The loop runs through range() from *start* up to but not including *stop*.

Let's look at some examples. 

<details class = "bordered-block">
    <summary>
        <code>range(3)</code>
        <code>range(1, 8, 3)</code>
    </summary>
    <div class = "inside-bordered-block">
        <ol>
            <li><code>range(3)</code> starts at 0, ends by 3, and increases by 1:</li>
                <ul>
                    <li> 0 < 3 True  -> add 1 to 0, get 1 </li>
                    <li> 1 < 3 True  -> add 1 to 1, get 2 </li>
                    <li> 2 < 3 True  -> add 1 to 2, get 3 </li>
                    <li> 3 < 3 False -> stop </li>
                </ul>
                So <code>range(3)</code> contains 0, 1, and 2.
            <li><code>range(1, 8, 3)</code> starts at 1, ends by 8, and increases by 3: </li>
                <ul>
                    <li> 1 < 8 True   -> add 3 to 1, get 4 </li>
                    <li> 4 < 8 True   -> add 3 to 4, get 7 </li>
                    <li> 7 < 8 True   -> add 3 to 7, get 10 </li>
                    <li> 10 < 8 False -> stop </li>
                </ul>
                So <code>range(1, 8, 3)</code> contains 1, 4, and 7.
        </ol>
    </div>
</details>

<details class = "bordered-block">
    <summary>
        <code class = "multi-line-code">
            # repeat 3 times
            for i in range(3):
                print(i)
        </code>
    </summary>
    <div class = "inside-bordered-block">
        <code>range(3)</code> contains 0, 1, and 2.
        <ul>
            <li> i = 0 < 3 True  -> print(0) -> add 1 to i = 0, get i = 1 </li>
            <li> i = 1 < 3 True  -> print(1) -> add 1 to i = 1, get i = 2 </li>
            <li> i = 2 < 3 True  -> print(2) -> add 1 to i = 2, get i = 3 </li>
            <li> i = 3 < 3 False -> stop -> display the rendered output </li>
        </ul>
        The rendered output is <br>
        0 <br>
        1 <br>
        2
    </div>
</details>

<details class = "bordered-block">
    <summary>
        <code class = "multi-line-code">
            # repeat 3 times
            value = 5
            for i in range(3):
                print(value + i)
        </code>
    </summary>
    <div class = "inside-bordered-block">
        <code>range(3)</code> contains 0, 1, and 2.
        <ul>
            <li> i = 0 < 3 True  -> print(5 + 0) -> add 1 to i = 0, get i = 1 </li>
            <li> i = 1 < 3 True  -> print(5 + 1) -> add 1 to i = 1, get i = 2 </li>
            <li> i = 2 < 3 True  -> print(5 + 2) -> add 1 to i = 2, get i = 3 </li>
            <li> i = 3 < 3 False -> stop -> display the rendered output </li>
        </ul>
        The rendered output is <br>
        5 <br>
        6 <br>
        7
    </div>
</details>

<details class = "bordered-block">
    <summary>
        <strong>Accumulation Pattern for Summation</strong>
        <code class = "multi-line-code">
            # repeat 5 times
            total = 0
            for i in range(5):
                total = total + i
                print(total)
        </code>
    </summary>
    <div class = "inside-bordered-block">
        <code>range(5)</code> contains 0, 1, 2, 3, and 4.
        <ul>
            <li> i = 0 < 5 True  -> total = 0 + 0 -> print(0)  -> add 1 to i = 0, get i = 1 </li>
            <li> i = 1 < 5 True  -> total = 0 + 1 -> print(1)  -> add 1 to i = 1, get i = 2 </li>
            <li> i = 2 < 5 True  -> total = 1 + 2 -> print(3)  -> add 1 to i = 2, get i = 3 </li>
            <li> i = 3 < 5 True  -> total = 3 + 3 -> print(6)  -> add 1 to i = 3, get i = 4 </li>
            <li> i = 4 < 5 True  -> total = 6 + 4 -> print(10) -> add 1 to i = 4, get i = 5 </li>
            <li> i = 5 < 5 False -> stop -> display the rendered output </li>
        </ul>
        The rendered output is <br>
        0 <br>
        1 <br>
        3 <br>
        6 <br>
        10 
    </div>
</details>

<details class = "bordered-block">
    <summary>
        <strong>Practice Exercises</strong>
    </summary>
    <div class = "inside-bordered-block language-python">
        <strong>Exercise 1: How is this loop different from the summation example?</strong>
        <code class = "multi-line-code">
            for i in range(5):
                total = 0
                total = total + i
            print(total)
        </code> <br>
        <strong>Exercise 2: Do these for loop calculate the  product of numbers from 1 to 5 (i.e., the factorial of 5)? Explain why or why not. How can you make a loop that uses accumulation pattern to compute the product?</strong>
        <code class = "multi-line-code">
            product = 0
            for i in range(5):
                product = product * i
            print(product)
        </code>
        <code class = "multi-line-code">
            product = 1
            for i in range(5):
                product = product * i
            print(product)
        </code> <br>
        <strong>Exercise 3: What should you pay attention to when applying the accumulation pattern?</strong>
    </div>
</details>

<hr>

## :mag_right: **While Loops**
```python
# code before while

while condition:
    # code inside while

# code after while
```
- A `while` loop is used when you don't know the number of iterations.
- A `while` loop runs as long as conditions are fulfilled.

:warning: Always update the condition inside the loop. Or else, you may create/encounter an infinite loop which runs forever unless you force it to quit.

<details class = "bordered-block">
    <summary>
        <code class = "multi-line-code">
            num= 1
            a_list = [0, 1, 2]
            while True == num in a_list:
                print("helloaivietnam")
                # remove the last time of a_list
                a_list.pop()
        </code>
    </summary>
    <div class = "inside-bordered-block">
        <ul>
            <li> num = 1 in [0, 1, 2] True -> (True == True) True    -> print("helloaivietnam") -> remove 2 -> a_list = [0, 1] </li>
            <li> num = 1 in [0, 1]    True -> (True == True) True    -> print("helloaivietnam") -> remove 1 -> a_list = [0] </li>
            <li> num = 1 in [0]       False -> (False == True) False -> go out of loop -> display the rendered output </li>
        </ul>
        The rendered output is <br>
        helloaivietnam <br>
        helloaivietnam 
    </div>
</details>

<hr>

## :mag_right: **Keywords Used in Loops**
### :stop_sign: **break**
Imagine you are playing "Where's Waldo?" and know exactly how many objects there are on the page. You want to look through images and stop the puzzle once you find Waldo. Or you could imagine you are playing a game where you have to guess a number. You'd have to keep guessing until you get the correct one; this is when the program would release you. Clearly, you need to use a loop to do iterations in each case, but you want a stronger command to leave when getting what you came for. Yup, it's a break!

The `break` keyword is mainly used to terminate a loop regardless of whether a for loop iterates over the whole range or the condition in a while loop may still be satisfied. Simply put, when the program hits the `break` keyword, it *breaks out* of the loop.

<details class = "bordered-block">
    <summary>
        <strong>Practice Exercises</strong>
    </summary>
    <div class = "inside-bordered-block language-python">
        <strong>Exercise 1: Do these for loops give the same result? Explain why or why not.</strong>
        <code class = "multi-line-code">
            for x in range(7):
                print(x)
                if x == 5:
                    break
            print("iloveaivietnam")
        </code>
        <code class = "multi-line-code">
            for x in range(7):
                if x == 5:
                    break
                print(x)
            print("iloveaivietnam")
        </code> <br>
        <strong>Exercise 2: Understand this while loop and modify itp to see how many times you need to roll a dice to get a six.</strong>
        <code class = "multi-line-code">
            import random
            while True:
                roll = random.randint(1, 6)
                print(f"You rolled {roll}.)
                if roll == 6:
                    print("You win!")
                    break
        </code> <br>
        <strong>Exercise 3: Give an example where you can replace a for loop with break with a while loop?</strong> 
        <br>
        <strong>Exercise 4: In which case do you think you have to use while loop with break instead of whle loop? Can you give an example?</strong>
    </div>
</details>

<br>
### :repeat: **continue**
Imagine you are building a machine that would pick up only ripe strawberries and skip unripe ones without stopping. Here, a loop can handle part of the requirements and needs a magic word to fulfil what you expect. Such a magic word is continue. 

The `continue` keyword is to skip the rest of the steps in this turn and move on with the next round. It is less common than the `break` keyword.

<details class = "bordered-block">
    <summary>
        <strong>Practice Exercises</strong>
    </summary>
    <div class = "inside-bordered-block language-python">
        <strong>Exercise 1: In which case is the continue keyword needed?</strong>
        <code class = "multi-line-code">
            for x in range(7):
                print(x)
                if x == 5:
                    continue
            print("iloveaivietnam")
        </code>
        <code class = "multi-line-code">
            for x in range(7):
                if x == 5:
                    continue
                print(x)
            print("iloveaivietnam")
        </code> <br>
        <strong>Exercise 2: Rewrite the code of exercise 1 with the while loop.</strong>
    </div>
</details>

<hr>

## :mag_right: **Examples and Exercises**
<details class = "bordered-block">
    <summary>
        <strong>Estimate the Area of a Unit Circle.</strong>
    </summary>
    <div class = "inside-bordered-block">
        ***Problem: Design a Loss Function***
        p is probability of something happen
        need to design function f(p) describes the level of your surprise. the more surprised you are, the larger f(p) is.
        experiment 2 is more surprising
        probability of something happen or our believe is disproportional to the level of your surprises i.e reciprocal
        the lower probability, the more surprised
        can be 1-p (linear) or 1/p(nonlinear) but nonlinear has more interesting properties/characteristics. so we choose s=surprise=1/p
        p in [0,1] so s=1/p in [1, inf] done
        however, we still want to improve this because s=1/p in [1,inf] means s=1/p=1 means no surprise, which is mathematically strange. we want s=0. we can minus, but come on, we have something better and we learned just now. yup it's logarithm. so we choose f(p)=log(1/p) in [0, inf] thanks to its monotone, f(p) is proportional to s=surprise=1/p which means both can interpret the level of surprise
        let's look deeper. in terms of graph, when taking log, the result is significantly lower (smaller) than s=1/p or 1/p-1, which is more convinient to compute.
        this function is called information used in information theory
    </div>
</details>

<details class = "bordered-block">
    <summary>
        <strong>Practice Exercises</strong>
    </summary>
    <div class = "inside-bordered-block language-python">
        <strong>Exercise 1: PI Estimation (Gregory-Leibniz Series)</strong>
        $$\text{PI} \approx 4 \sum_{i=1}^{n} \frac{(-1)^{i+1}}{2i-1}$$ <br>
        <strong>Exercise 2: PI Estimation (Nilakantha Series)</strong>
        $$\text{PI} \approx 3 + 4 \sum_{i=0}^{n} \frac{(-1)^{i}}{(2i+2)(2i+3)(2i+4)}$$ <br>
        <strong>Exercise 3: Quadratic Root (Newton Method)</strong> <br>
        The Newton Method can be used to approximate a root of $f(x) = 0$ by the general formula 
        $$x_{n+1} = x_{n} - \frac{f(x_n)}{f'(x_{n})}.$$
        Generate a mathematical formula for $f(x) = x^2 - a$ to approximate $\sqrt{a}$. Rewrite the formula in a programming fashion. Write a function that $a$ and $n$ and returns $\sqrt{a}$. 
        <br>
        <strong>Exercise 4: Write a function the compute the factorial of a positive number using a loop.</strong>
        <br>
        <strong>Exercise 5: Write a function the compute the factorial of a non-negative number using a loop.</strong>
        <br>
        <strong>Exercise 6: Give possible mistakes one might make when writing a factorial function.</strong>
    </div>
</details>

<hr>

## :speech_balloon: **FAQ**
Here are some good questions asked during class. The answers to these questions came from either some admin, TA, STA, or members of AIO2025.

| Questions | Answers |
|-----------|---------|
| Can I call a function before defining it? | No. Python reads code top-down. |
| Can I write 2 functions A and B where A calls B and B calls A? Why does it give an error when I refer to one before defining it? | Yes. Such a situation is called **mutual recursion**. Python doesn't execute function definitions which consist of function references. It only starts evaluating when a function is called. |

<br>

#### :clipboard: Check List

- [x] Exercise 1 (date)
- [ ] Exercise 2 (date)
  - [x] Put on left sock
  - [ ] Put on right sock
- [x] Go to school
